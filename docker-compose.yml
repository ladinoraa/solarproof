services:
  web:
    build: apps/web
    ports:
      - "3000:3000"
    env_file: apps/web/.env.example
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
